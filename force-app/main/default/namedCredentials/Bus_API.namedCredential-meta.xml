<?xml version="1.0" encoding="UTF-8"?>
<!--
  Points at the RoadLink Bus API (bus-booking-app/api), deployed on Render.
  BusApiClient.cls calls this via `callout:Bus_API/api/...` for booking
  lookup, reschedule/cancel eligibility, and performing reschedules/
  cancellations.

  Auth: protocol is NoAuthentication - BusApiClient sets the x-api-key header
  itself on every request, reading the key from the Bus_Api_Config__mdt
  "Default" record (Setup > Custom Metadata Types > Bus Api Config > Manage
  Records), which must match the API_KEY env var set on the Render service.
-->
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <calloutStatus>Enabled</calloutStatus>
    <endpoint>https://roadlink-bus-api.onrender.com</endpoint>
    <generateAuthorizationHeader>false</generateAuthorizationHeader>
    <label>Bus API</label>
    <principalType>Anonymous</principalType>
    <protocol>NoAuthentication</protocol>
</NamedCredential>
