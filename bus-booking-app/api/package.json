{
  "name": "bus-booking-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "External Bus API for RoadLink — bus search, seats, bookings, cancellation, rescheduling.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --experimental-sqlite --watch src/server.js",
    "start": "node --experimental-sqlite src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
